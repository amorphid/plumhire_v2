<div class="page-header">
  <h1 id="forms">
    <% if @application.new_record? %>
      New
    <% else %>
      Edit
    <% end %>
    job
  </h1>
</div>
<div class="well bs-component">
  <%= render("form") %>
</div>
